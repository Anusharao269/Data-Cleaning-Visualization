# Data-Cleaning-Visualization
This project focuses on data cleaning, preprocessing, and visualization using Python. The goal is to transform a raw dataset into a clean and meaningful format and generate insights through visual analysis.  The dataset initially contains issues such as missing values, duplicate records, and outliers. 
